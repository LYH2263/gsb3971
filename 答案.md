# 学员生命周期与铺位占用实现分析

> 涉及文件：后端 [CustomerService.java](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java)、[RoomService.java](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/RoomService.java)；前端 [CustomersView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/CustomersView.vue)、[RoomsView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/RoomsView.vue)、[validators.ts](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/utils/validators.ts)。
>
> 两套枚举不要混淆：学员状态 `DRAFT / RESIDENT / OUTING / DISCHARGED`；铺位状态 `AVAILABLE / OCCUPIED / DISABLED`。

## 一、状态机（前置状态、校验顺序与失败语义）

1. **动作分发**：三个动作在 `applyLifecycle` 中按 `action` 字符串分发（先 `requireLogin`、查学员，学员不存在抛 404 `RESOURCE_NOT_FOUND`），未知动作抛 400 `VALIDATION_ERROR`。

2. **入营 `checkin`（关键：不校验学员当前状态）**。它只按顺序校验铺位，从不检查 `customer.getStatus()`，因此任何状态都能触发入营，校验顺序为：
   - `bedId == null` → 400 `VALIDATION_ERROR`「入营必须选择铺位」；
   - 目标铺位不存在 → 404 `RESOURCE_NOT_FOUND`「铺位不存在」；
   - 目标铺位 `DISABLED` → 409 `BUSINESS_CONFLICT`「铺位已停用」；
   - 目标铺位被他人占用 → 409 `BED_OCCUPIED`「铺位已占用」；
   - 通过后：释放旧铺（若有且非 `DISABLED`）→ 目标铺位置 `OCCUPIED` → 学员置 `RESIDENT`、写 `bedId` 与 `checkinDate`。

3. **结营 `discharge`**：前置状态必须 ∈ `{RESIDENT, OUTING}`，否则 409 `CUSTOMER_STATUS_INVALID`「当前状态不允许结营」；随后释放铺位（非 `DISABLED` 才置 `AVAILABLE`）、学员置 `DISCHARGED`、`bedId` 清空。

4. **离营外出 `outing`**：前置状态必须 = `RESIDENT`（`OUTING/DRAFT/DISCHARGED` 均不可），否则 409 `CUSTOMER_STATUS_INVALID`「状态不允许离营外出，仅在营学员可外出」；只改学员状态为 `OUTING`，**不动 `bedId`、不动铺位状态**。

5. **三种入营场景的实际结果**（因入营不校验学员状态）：
   - **`DRAFT` 直接入营**：`oldBedId` 为空 → 无旧铺释放，目标铺 `OCCUPIED`、学员 `RESIDENT`，正常成功。
   - **`OUTING` 再入营（换铺）**：允许。选新铺时旧铺释放为 `AVAILABLE`（除非旧铺 `DISABLED`），学员由 `OUTING` 重新回到 `RESIDENT`——即“外出→再入营”会顺带换铺并复位。占用判定用 `.ne(id)` 排除自身，故自己 `OUTING` 占的旧铺不会挡住自己。
   - **已结营（`DISCHARGED`）再次入营**：允许。结营时 `bedId` 已清空，故无旧铺释放，直接占新铺、状态回到 `RESIDENT`，相当于重新入营。

## 二、「铺位被占用」判定与占用一致性

1. **入营冲突判定依据的是「学员状态集合」，不是铺位 `OCCUPIED` 字段**：查询 `bedId = 目标铺 且 status ∈ {RESIDENT, OUTING} 且 id ≠ 自己`，命中即 `BED_OCCUPIED`。铺位自身的 `OCCUPIED` 字段只是冗余展示/停用判据，不参与入营冲突判断。

2. **`listRooms` 的占用学员展示同样以学员状态集合为准**：查 `bedId 非空 且 status ∈ {RESIDENT, OUTING}`，映射到床位上显示占用人。判定**口径一致**（同一状态集合 `RESIDENT+OUTING`）；唯一差别是入营会 `.ne(自身)` 排除本人，而 `listRooms` 是纯展示不排除任何人。

3. **离营外出仍占铺**：因为 `OUTING` 属于占用状态集合，且 `outing()` 保留 `bedId`、不改铺位状态；因此外出学员在入营冲突判定和 `listRooms` 展示中都仍算占用该铺。

## 三、旧铺释放、`DISABLED` 特殊处理与「占用铺位停用」的拦截点

1. **入营换铺释放旧铺**：仅当 `oldBedId != null 且 ≠ 目标铺` 时释放；且**旧铺为 `DISABLED` 时跳过**（保持停用，不回写 `AVAILABLE`）。

2. **结营释放铺位对 `DISABLED` 的处理**：同样是「非 `DISABLED` 才置 `AVAILABLE`」——`DISABLED` 铺位被保留原状，避免把管理员停用的铺误置为空闲。

3. **对占用中铺位执行停用的拦截**：
   - **后端 `saveBed`**：当 `请求状态 = DISABLED 且 铺位当前 = OCCUPIED` 时抛 409 `BUSINESS_CONFLICT`「占用中的铺位不可停用」——这是**唯一的实际拦截点**。
   - **前端 `RoomsView`**：`changeBedStatus` 与「设为停用」下拉项**无任何守卫**，占用中的铺位照样可点击发起请求，只能靠后端拒绝后在 `catch` 中 `ElMessage.error` 提示。

## 四、前端「离营外出」按钮禁用 vs 后端 `outing` 校验

1. **逻辑等价**：前端按钮 `:disabled="row.status !== 'RESIDENT'"`，后端 `outing` 也要求 `= RESIDENT`，判定条件相同。

2. **但前端未复用 `validators.canOuting`**：`canOuting` 定义了等价的 `status === 'RESIDENT'`，`CustomersView` 却内联判断，未引用该校验器（存在重复/失联风险）。

3. **仅一侧能拦的例子（后端兜底）**：并发场景下页面数据陈旧——列表里 `row.status` 仍是 `RESIDENT` 但该学员已被他处改成 `OUTING/DISCHARGED`，此时**前端按钮可点、放行**，只有**后端**校验命中并抛 409 `CUSTOMER_STATUS_INVALID`。反向（前端能拦而后端放行）不存在，说明后端才是权威门控，前端仅为体验优化。

---

| 类名 / 文件 | 方法名 | 关键校验点 |
| --- | --- | --- |
| CustomerService | checkin | 不校验学员状态；顺序：铺位非空→存在→非 DISABLED→按 {RESIDENT,OUTING}∧≠自身 判占用；换铺释旧铺(非DISABLED) |
| CustomerService | discharge | 前置 ∈ {RESIDENT,OUTING} 否则 CUSTOMER_STATUS_INVALID；释铺仅非 DISABLED，清 bedId |
| CustomerService | outing | 前置必须 = RESIDENT；仅改状态为 OUTING，保留 bedId/铺位状态（仍占铺） |
| RoomService | listRooms | 占用展示按学员 {RESIDENT,OUTING}∧bedId 非空，与入营口径一致（不排除自身） |
| RoomService | saveBed | DISABLED∧当前 OCCUPIED → 拒绝「占用中不可停用」；铺位号同房唯一 |
| CustomersView.vue | 离营外出按钮 | disabled = status!=='RESIDENT'；内联判断，未复用 canOuting |
| RoomsView.vue | changeBedStatus | 无停用守卫，占用铺可发起请求，依赖后端拒绝 |
| validators.ts | canOuting | status === 'RESIDENT'，与后端等价但前端未引用 |
