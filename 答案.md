# 学员生命周期与铺位占用：实现分析

后端入口：[CustomerService.java](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java)、[RoomService.java](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/RoomService.java)；前端入口：[CustomersView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/CustomersView.vue)、[validators.ts](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/utils/validators.ts)。状态常量见 [CustomerStatus.java](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/constant/CustomerStatus.java)（DRAFT/RESIDENT/OUTING/DISCHARGED）与 [BedStatus.java](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/constant/BedStatus.java)（AVAILABLE/OCCUPIED/DISABLED）。

## 结论

1. **状态机与三个动作的校验顺序（[CustomerService.applyLifecycle](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java#L117-L132)，整体 `@Transactional`，任一步抛错全部回滚）**
   1. 统一前置：先 `requireLogin()`（未登录 401/40102）；按 id 查学员，不存在 → 404/40401「学员不存在」；`action` 转小写后 switch，非法动作 → 400/40001「无效的生命周期动作」。Bean Validation 另兜底：`action` 不能为空、`actionDate` 不能为 null（400/40001）。
   2. **checkin 入营**（[L134-L172](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java#L134-L172)）——注意它**没有任何学员状态前置校验**，校验顺序为：① `bedId` 为 null → 400/40001「入营必须选择铺位」；② 目标铺不存在 → 404/40401「铺位不存在」；③ 目标铺 `status=DISABLED` → 409/40904「铺位已停用」；④ 统计 `bed_id=目标铺 AND status IN (RESIDENT,OUTING) AND id<>自己` 的学员数，>0 → 409/40902「铺位已占用」；⑤ 释放旧铺；⑥ 目标铺置 OCCUPIED、学员置 RESIDENT、`bedId` 指向新铺、`checkinDate` 覆盖为本次 `actionDate`。
   3. **discharge 结营**（[L174-L192](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java#L174-L192)）：仅 RESIDENT、OUTING 允许，否则 409/40903「当前状态不允许结营」（即外出未归可直接结营）；随后释放铺位、学员置 DISCHARGED 且 `bedId=null`（`checkinDate` 保留）。
   4. **outing 离营外出**（[L194-L206](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java#L194-L206)）：仅 RESIDENT 允许，否则 409/40903「状态不允许离营外出，仅在营学员可外出」；只把学员置 OUTING，**不动 `bedId`、不动铺位状态**。
   5. 状态迁移图：`DRAFT --checkin--> RESIDENT`；`RESIDENT --outing--> OUTING`；`OUTING --checkin--> RESIDENT`（系统无独立"归营"动作，归队即再入营）；`RESIDENT/OUTING --discharge--> DISCHARGED`。非法迁移：DRAFT 调 outing/discharge、OUTING 再调 outing、DISCHARGED 调 outing/discharge 均 409/40903。
   6. 三种特殊入营：**DRAFT 直接入营**是标准路径（建档时无铺位，无旧铺可释放，校验通过后占铺、写入住日期）；**OUTING 再入营换铺**完全合法——占用计数用 `.ne(id, 自己)` 排除本人，本人在旧铺上的 OUTING 记录不算冲突，旧铺按第 3 条规则释放、新铺置 OCCUPIED、状态回 RESIDENT、`checkinDate` 被覆盖；选原铺则不触发释放、铺位重写 OCCUPIED。**已结营再次入营后端不拦截**：discharge 已把 `bedId` 置 null，checkin 又不校验学员状态，故 DISCHARGED 学员选一张空闲铺即可成功回到 RESIDENT（`checkinDate` 被新日期覆盖）——结营并非终态，这是状态机的一个缺口；前端「入营」按钮对任何状态都不禁用，UI 同样放行。
2. **「铺位被占用」的判定依据（[checkin L146-L152](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java#L146-L152)）**
   1. 判定依据是**学员状态集合**，不是铺位的 OCCUPIED 字段：冲突查询查的是「挂在该铺上、状态为 RESIDENT 或 OUTING、且不是本人」的学员行数。`beds.status` 只是生命周期操作顺手维护的冗余镜像（checkin 写 OCCUPIED，结营/换铺写 AVAILABLE），占用判定从不读它。数据库层 `customers.bed_id` 只有外键、**没有唯一约束**（见 [001_schema.sql](file:///d:/Agsb731/Agsb825/gsb3971/ops/mysql/init/001_schema.sql#L38-L50)），"一铺一人"完全靠服务层这条 count 查询兜底。
   2. [RoomService.listRooms](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/RoomService.java#L47-L94) 展示占用学员用的是**同一口径**：另查 `bed_id IS NOT NULL AND status IN (RESIDENT,OUTING)` 的学员，按 `bedId` 映射成 BedView 的 customerId/customerName（同铺多人时取第一条）。但两者并非完全一致：① listRooms 不排除本人（展示场景无需排除）；② 它不看 `beds.status`，而 checkin 在占用计数之前还额外拦 DISABLED 铺；③ BedView 的 `status` 字段原样透传铺位状态。因此两者可能漂移：管理员可经 saveBed 把 OCCUPIED 铺手动改成 AVAILABLE（服务层只拦"停用"不拦"设空闲"），此时铺位字段是 AVAILABLE、却仍有 RESIDENT 学员挂着——listRooms 仍显示该学员占用，他人入营该铺仍报 40902；反向（铺位 OCCUPIED 但无活跃学员）时入营判定视为空铺、可直接入住并覆盖写 OCCUPIED。
   3. **离营外出仍占铺**，依据有二：outing() 不释放铺位（`bedId` 与铺位 OCCUPIED 状态都保持）；且占用集合（checkin 计数与 listRooms 查询）都把 OUTING 与 RESIDENT 并列计入。外出期间他人不能入住该铺，学员列表仍显示其铺位号。
3. **换铺/结营的旧铺释放与 DISABLED 特殊处理；停用占用铺的拦截分层**
   1. 入营换铺时（[L154-L161](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java#L154-L161)）：`oldBedId` 非空且不等于新铺时加载旧铺，**仅当旧铺存在且状态不是 DISABLED** 才置 AVAILABLE；旧铺已是 DISABLED 则跳过、保持 DISABLED——避免管理员主动停用的铺位因学员换铺被"复活"。
   2. 结营释放（[L182-L186](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/CustomerService.java#L182-L186)）规则相同：当前铺非 DISABLED 才置 AVAILABLE，DISABLED 铺保持 DISABLED；但无论铺位状态如何，学员 `bedId` 都会置 null。
   3. 对占用中铺位执行停用，**唯一硬拦截在服务层** [RoomService.saveBed](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/service/RoomService.java#L170-L175)：更新铺位时若 `请求状态=DISABLED 且 库中现状=OCCUPIED` → 409/40904「占用中的铺位不可停用」。控制器层（[RoomController](file:///d:/Agsb731/Agsb825/gsb3971/backend/src/main/java/com/hanyu/learning/controller/RoomController.java#L39-L42)）无任何校验，DB 也无约束。
   4. 前端 [RoomsView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/RoomsView.vue#L137-L159) **不拦截**："调整铺位状态"下拉对每张铺都渲染"设为空闲/设为停用"两个菜单项，无 disabled 绑定，点击直接发请求，错误仅靠 toast 回显（e2e 用例 [03-rooms-beds.spec.ts](file:///d:/Agsb731/Agsb825/gsb3971/e2e/specs/03-rooms-beds.spec.ts#L64-L66) 正是断言 toast 文案）。
   5. 该拦截看的是 **`beds.status` 字段而非学员占用集合**，存在盲区：它只拦 `OCCUPIED→DISABLED`，不拦 `OCCUPIED→AVAILABLE`，也不拦 AVAILABLE→DISABLED。若铺位已被改成 AVAILABLE 但仍有 RESIDENT/OUTING 学员挂着（第 2.2 条的漂移态），停用请求会被放行，学员就此挂在 DISABLED 铺上；此后该学员结营/换铺时，释放逻辑又因 DISABLED 跳过，铺位一直停用。另注意 saveBed/saveRoom 要求管理员（`requireAdmin`，非管理员 403/40301），生命周期接口仅要求登录。
4. **前端「离营外出」门控与后端 outing 校验的差异（[CustomersView.vue L216-L224](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/CustomersView.vue#L216-L224)、[validators.ts L7-L9](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/utils/validators.ts#L7-L9)）**
   1. 状态语义上**等价**：按钮 `:disabled="row.status !== 'RESIDENT'"` 与后端「仅 RESIDENT 可 outing」完全一致；[canOuting](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/utils/validators.ts#L7-L9) 的逻辑（`status === 'RESIDENT'`）也相同。但 canOuting **并未被任何视图引用**（仅 [validators.test.ts](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/utils/validators.test.ts) 使用），视图是内联表达式，规则重复两处、靠人工保持一致。
   2. 运行时保证上**不等价**：前端禁用依据的是列表接口的快照（挂载/操作后才刷新），后端依据数据库当前状态；前端也不校验登录态（token 失效由请求层兜 401）；`quickOuting` 另有一层 ElMessageBox 确认框，取消则不发请求。
   3. **仅一侧能拦住的例子（仅后端拦得住）**：学员已被他人或另一个浏览器标签页登记为 OUTING/DISCHARGED，而当前页面未刷新、该行仍显示"在营"，按钮可点击；确认提交后后端返回 409/40903。绕过页面直接调 API（脚本/curl）同理。反向不存在状态级差异——前端禁用的 DRAFT/OUTING/DISCHARGED，后端也一律 409，没有"前端拦而后端放行"的状态组合。
   4. 同源的更大口径差异在入营铺位下拉：前端 `availableBeds` 用 **`bed.status === 'AVAILABLE'`** 过滤（[L49](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/CustomersView.vue#L49)），后端占用判定用**学员状态集合**。漂移态下（铺 AVAILABLE 但有在营学员）下拉会展示该铺，选中提交后被后端 40902 拒绝——同样是"仅后端拦得住"；而 DISABLED 铺前端下拉不展示、后端也以 40904 拒绝，这一侧两边一致。

## 关键校验点一览

| 类名 | 方法名 | 关键校验点 |
| --- | --- | --- |
| CustomerService | applyLifecycle | requireLogin；学员不存在 404/40401；非法 action 400/40001；`@Transactional` 统一回滚 |
| CustomerService | checkin | 不校验学员状态；bedId 非空(400)→铺位存在(404)→非 DISABLED(409/40904)→占用计数 RESIDENT+OUTING 且排除自己(409/40902)→旧铺非 DISABLED 才释放→新铺 OCCUPIED、学员 RESIDENT、checkinDate 覆盖 |
| CustomerService | discharge | 仅 RESIDENT/OUTING 可结营(409/40903)；当前铺非 DISABLED 才置 AVAILABLE；学员置 DISCHARGED、bedId 置 null |
| CustomerService | outing | 仅 RESIDENT 可外出(409/40903)；只改学员状态，bedId 与铺位 OCCUPIED 均不变（外出仍占铺） |
| RoomService | listRooms | 占用学员取 `bed_id 非空 AND status IN (RESIDENT,OUTING)` 按 bedId 映射；bed.status 原样透传，不排除本人、不看 OCCUPIED 字段 |
| RoomService | saveBed | requireAdmin；铺位号房间内唯一(409/40904)；仅拦 `OCCUPIED→DISABLED`（「占用中的铺位不可停用」），不拦 OCCUPIED→AVAILABLE；返回时按学员集合回填占用学员 |
| CustomersView（前端） | 按钮/下拉门控 | 入营按钮无状态禁用；外出按钮 `status!=='RESIDENT'` 禁用；结营按钮非 RESIDENT/OUTING 禁用；入营铺位下拉仅显示 `bed.status==='AVAILABLE'` |
| validators（前端） | canOuting | `status==='RESIDENT'`，与按钮内联规则等价但视图未引用（仅单测覆盖），属重复规则 |
