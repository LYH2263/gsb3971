# 学员生命周期与铺位占用实现分析

## 结论

1. **状态机总览**：学员有四种状态 `DRAFT`（待入营）、`RESIDENT`（在营）、`OUTING`（离营外出）、`DISCHARGED`（已结营）。三个动作的校验顺序如下：
   - **入营 `checkin`**：方法本身**不校验学员当前状态**，任何状态均可发起入营。校验顺序为：① `bedId` 非空（400 `VALIDATION_ERROR`「入营必须选择铺位」）→ ② 目标铺位存在（404 `RESOURCE_NOT_FOUND`）→ ③ 目标铺位非 `DISABLED`（409 `BUSINESS_CONFLICT`「铺位已停用」）→ ④ 查询是否有**其他** `RESIDENT`/`OUTING` 学员占着该铺（409 `BED_OCCUPIED`「铺位已占用」）。全部通过后才释放旧铺、占用新铺、置学员为 `RESIDENT`。
   - **结营 `discharge`**：仅允许 `RESIDENT` 或 `OUTING`，否则抛 409 `CUSTOMER_STATUS_INVALID`「当前状态不允许结营」。随后释放铺位（非 `DISABLED` 则置 `AVAILABLE`），学员置 `DISCHARGED` 并清空 `bedId`。
   - **离营外出 `outing`**：仅允许 `RESIDENT`，否则抛 409 `CUSTOMER_STATUS_INVALID`「状态不允许离营外出，仅在营学员可外出」。仅改学员状态，**不动铺位状态**。

2. **三种特殊入营场景的行为**：
   - **`DRAFT` 直接入营**：最常规路径。`oldBedId` 为 `null`，不涉及旧铺释放；目标铺位置 `OCCUPIED`，学员变 `RESIDENT`，写入 `checkinDate`。
   - **`OUTING` 再入营换铺**：后端不拦截状态。若新铺与旧铺不同，旧铺（非 `DISABLED` 时）被置回 `AVAILABLE`，新铺置 `OCCUPIED`，学员回到 `RESIDENT` 并更新 `checkinDate`。若想选回**原铺**，后端允许（占用查询用 `.ne(id, currentId)` 排除了自己），但前端下拉框只列 `AVAILABLE` 铺位，原铺仍是 `OCCUPIED`，UI 上选不到。
   - **已结营再次入营**：结营时 `bedId` 已被清空，所以 `oldBedId` 为 `null`，行为与 `DRAFT` 入营几乎一致——选一个可用铺位、置 `RESIDENT`、写入新的 `checkinDate`。后端没有「结营后不可再入营」的限制。

3. **「铺位被占用」的判定依据**：入营冲突判定查的是**学员状态集合**（`CustomerEntity.status IN (RESIDENT, OUTING)` 且 `bedId = 目标铺` 且 `id != 当前学员`），而**不是**读取 `BedEntity.status == OCCUPIED` 字段。铺位的 `OCCUPIED` 字段只是一个冗余/展示用标记，真正的占用事实来源是学员表。

4. **`listRooms` 展示占用学员与入营冲突判定的一致性**：两者使用的**状态集合完全一致**（都是 `RESIDENT` + `OUTING`、`bedId IS NOT NULL`），因此「谁占着铺」的语义相同。差异在于：入营判定多了一个 `.ne(id, 当前学员)` 排除自身（用于换铺/再入营时不把自己当冲突），而 `listRooms` 是纯展示不需要排除；此外 `listRooms` 仍把 `bed.status` 原样返回给前端，与动态算出的占用学员并列，可能出现「铺位状态字段是 `AVAILABLE` 但实际挂着一个 `OUTING` 学员」之类的不一致（正常流程下不会出现，但字段不是真相源）。

5. **离营外出是否仍占铺**：**仍占铺**。依据有两点：① `outing()` 方法只更新学员状态为 `OUTING`，既不改 `bedId`，也不改 `BedEntity.status`（铺位保持 `OCCUPIED`）；② 入营占用查询和 `listRooms` 展示查询都把 `OUTING` 计入占用集合。因此外出学员的铺位不会被分配给别人。

6. **入营换铺时旧铺释放规则**：当 `oldBedId != null` 且与目标铺不同时，加载旧铺：若旧铺存在且状态**不是** `DISABLED`，则置为 `AVAILABLE`；若旧铺已是 `DISABLED`，则**跳过不动**——因为该铺可能是在学员占用期间被管理员停用的（实际上正常流程下占用铺不能被停用，此处属于防御性代码），释放时不应把一个显式停用的铺位改回空闲。新铺一律置 `OCCUPIED`。

7. **结营释放铺位对 `DISABLED` 的处理**：与换铺逻辑一致——若当前铺位是 `DISABLED`，结营时**不把它改回 `AVAILABLE`**，只清空学员的 `bedId`。这样铺位维持停用态，不会因为学员结营而意外变成可分配。

8. **对占用中铺位执行停用的拦截层级**：
   - **服务层（后端）**：`RoomService.saveBed` 在更新铺位状态时，若传入 `DISABLED` 且当前 `bed.status == OCCUPIED`，直接抛 409 `BUSINESS_CONFLICT`「占用中的铺位不可停用」。这是唯一的硬拦截。
   - **前端**：[RoomsView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/RoomsView.vue) 的「调整铺位状态 → 设为停用」下拉项**没有任何禁用条件**，对所有铺位（含 `OCCUPIED`）都可点击，前端不拦截，完全依赖后端报错后 `ElMessage.error` 提示。
   - 注意后端这里判断的是 `bed.status == OCCUPIED` 字段，而非实时查学员表；在正常事务一致性下两者等价，但字段本身不是真相源。

9. **前端「离营外出」按钮与后端 `outing` 校验的等价性**：后端 `outing()` 要求 `status == RESIDENT`；前端按钮 `:disabled="row.status !== 'RESIDENT'"` 条件相同，[validators.ts](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/utils/validators.ts) 中的 `canOuting` 也只判断 `status === 'RESIDENT'`。**纯状态判断上等价**，但存在以下差异：
   - `canOuting` 函数**并未被 [CustomersView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/CustomersView.vue) 引入或调用**，按钮用的是内联表达式，validator 目前只在单元测试里被覆盖，属于未接线状态。
   - **仅后端能拦住的情况**：并发/脏数据场景。例如页面加载时学员为 `RESIDENT`（按钮可点），另一会话已将其结营为 `DISCHARGED`，当前用户再点「离营外出」，前端不会禁用，请求到达后端才被 409 `CUSTOMER_STATUS_INVALID` 拦下。前端基于的是加载时刻的快照，后端基于数据库实时状态。
   - **仅前端能拦住（后端反而放行）的情况**：`OUTING` 学员想「入营回到自己原来的铺位」。后端 `checkin` 不校验学员状态，且占用查询排除了自己，原铺可选；但前端入营下拉框 `availableBeds` 只过滤 `status === 'AVAILABLE'` 的铺位，原铺是 `OCCUPIED`，根本不会出现在下拉列表中，导致 UI 上无法完成「外出归来住回原铺」，必须换一个铺位。

## 关键校验点一览表

| 类名 | 方法名 | 关键校验点 |
| --- | --- | --- |
| CustomerService | checkin | bedId 非空 → 铺位存在 → 非 DISABLED → 无其他 RESIDENT/OUTING 学员占用；不校验学员自身状态 |
| CustomerService | discharge | 仅允许 RESIDENT/OUTING；释放铺位时跳过 DISABLED 铺位，清空 bedId |
| CustomerService | outing | 仅允许 RESIDENT；仅改学员状态，保留 bedId 与铺位 OCCUPIED 状态 |
| RoomService | listRooms | 以学员 status IN(RESIDENT,OUTING) 作为占用事实源回填 BedView，bed.status 仅作展示 |
| RoomService | saveBed | 更新铺位时若目标状态为 DISABLED 且当前为 OCCUPIED 则抛「占用中的铺位不可停用」 |
| CustomersView | availableBeds / 入营提交 | 下拉只列 status==='AVAILABLE' 的铺位；入营按钮无状态禁用；提交前校验 bedId 已选 |
| CustomersView | 离营外出/结营按钮 | outing 按钮禁用条件 `status!=='RESIDENT'`；discharge 按钮禁用条件 `status NOT IN(RESIDENT,OUTING)` |
| validators | canOuting | 仅判断 `customer.status === 'RESIDENT'`，与按钮内联逻辑一致但未被视图实际引用 |
