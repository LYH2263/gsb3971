# 学员生命周期与铺位占用 答案

## 一、状态机与校验顺序

1. **入口与分发**：`PATCH /customers/{id}/lifecycle` 由 `CustomerController` 进入 `CustomerService.applyLifecycle`。校验顺序固定为：`permissionService.requireLogin()` → 学员存在性（不存在 404 `RESOURCE_NOT_FOUND`）→ `action` 白名单分发（`checkin/discharge/outing`，其余 400 `VALIDATION_ERROR`）。整个方法标注 `@Transactional(rollbackFor = Exception.class)`，学员行与铺位行的多次写在同一事务内，任一步失败整体回滚，不会出现"铺位已改、学员未改"的半同步状态。

2. **checkin（入营）——对学员当前状态零前置校验**。`checkin()` 内部完全不检查 `customer.getStatus()`，因此 `DRAFT / RESIDENT / OUTING / DISCHARGED` 四种状态都允许入营。其校验链按序为：
   - `bedId` 必填 → 400 `VALIDATION_ERROR`（"入营必须选择铺位"）；
   - 铺位存在 → 404 `RESOURCE_NOT_FOUND`；
   - 铺位非 `DISABLED` → 409 `BUSINESS_CONFLICT`（"铺位已停用"）；
   - 铺位未被他人占用 → 409 `BED_OCCUPIED`（"铺位已占用"，注意占用判定在状态校验之后、任何写操作之前）。
   全部通过后依次写：释放旧铺 → 目标铺置 `OCCUPIED` → 学员置 `RESIDENT` 并写入 `bedId` 与 `checkinDate`。

3. **三个特例入营行为**：
   - `DRAFT` 直接入营：正常流程，无旧铺（`bedId` 为 null）可释放，目标铺直接 `OCCUPIED`，状态 `DRAFT → RESIDENT`。
   - `OUTING` 再入营换铺：允许。外出期间学员仍挂着旧 `bedId`，入营到不同铺时走换铺分支——旧铺置回 `AVAILABLE`（若旧铺非 `DISABLED`），新铺置 `OCCUPIED`，状态由 `OUTING` 回到 `RESIDENT`。入营到原铺则跳过旧铺释放，仅刷状态与日期。
   - 已结营（`DISCHARGED`）再次入营：同样放行，等价于"重新入营"。结营时已把 `bedId` 置 null，故无旧铺释放；状态 `DISCHARGED → RESIDENT`，`checkinDate` 被覆盖为新日期。这是该状态机里唯一能从终态"复活"的边。

4. **discharge（结营）**：唯一前置是学员状态 ∈ {`RESIDENT`, `OUTING`}，否则 409 `CUSTOMER_STATUS_INVALID`（"当前状态不允许结营"）。通过后释放当前铺（非 `DISABLED` 才置回 `AVAILABLE`），学员置 `DISCHARGED`、`bedId = null`（`checkinDate` 不清空）。

5. **outing（离营外出）**：唯一前置是学员状态 == `RESIDENT`，否则 409 `CUSTOMER_STATUS_INVALID`（"状态不允许离营外出，仅在营学员可外出"）。只改学员状态为 `OUTING`，**不动铺位表**。失败语义统一为 409 业务冲突，与 checkin 的参数类错误（400/404）区分开。

## 二、占用判定的依据与一致性

1. **入营的"铺位被占用"判定依据的是学员状态集合，不是铺位 `OCCUPIED` 字段**。`checkin()` 用 `count(customer where bedId = 目标铺 and status in (RESIDENT, OUTING) and id != 本人)` 判占用；`bed.status` 只用于拦 `DISABLED`。也就是说铺位行的 `OCCUPIED` 是"冗余展示字段"，真值在学员表里。

2. **`listRooms` 展示占用学员用的判定与入营冲突判定是同一集合**：`bedId 非空 and status in (RESIDENT, OUTING)`，按 `bedId` 聚合成 `bedCustomerMap` 挂到 `BedView.customerId/customerName` 上（同铺多学员时取先查到的一条）。两处集合一致，但展示端少了"排除本人"和计数，且展示字段是 `bed.status`——两者数据源不同会产生漂移：`bed.status = OCCUPIED` 但无在营/外出学员时，页面显示"占用"却查不到占用人，入营反而放行；反过来若铺被手工调成 `AVAILABLE` 而仍有 `RESIDENT/OUTING` 学员挂着，页面显示"空闲"且能进前端入营下拉，提交时才会被后端 `BED_OCCUPIED` 拦住。

3. **离营外出仍占铺**。依据有二：`outing()` 只改学员状态、不触碰铺位（铺保持 `OCCUPIED`）；且入营冲突判定与 `listRooms` 的占用集合都显式包含 `OUTING`。外出学员"人在营外、铺位保留"，只有结营或换铺入营才会释放。

## 三、换铺释放、`DISABLED` 特殊处理与停用拦截

1. **入营换铺的旧铺释放**：`checkin()` 在占用校验通过后，若 `oldBedId != null` 且不等于目标铺，则加载旧铺并置 `AVAILABLE`——但仅当旧铺状态不是 `DISABLED`。若旧铺已是 `DISABLED`（例如管理员趁学员在营时…实际上服务层拦了占用中停用，所以更常见于数据漂移或老数据），服务层选择"不碰它"，保持 `DISABLED`，避免把管理员停用的铺误恢复为可售。

2. **结营释放对 `DISABLED` 的同款处理**：`discharge()` 释放铺位时同样判断 `!DISABLED` 才置 `AVAILABLE`，停用铺结营后仍保持 `DISABLED`。两处语义一致：`DISABLED` 是人工管理状态，生命周期动作永不自动解除它。

3. **对占用中铺位执行停用，各层拦截位置**：
   - 前端 [RoomsView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/RoomsView.vue) **完全不拦**："调整铺位状态"下拉对所有铺（含 `OCCUPIED`）都提供"设为停用"，`changeBedStatus` 直接发请求；新增铺位弹窗的状态选项只有 `AVAILABLE/DISABLED`，也无一处按占用禁用按钮。
   - 服务层 `RoomService.saveBed` 拦在更新分支：仅当请求 `status = DISABLED` 且 `bed.status == OCCUPIED` 时抛 409 `BUSINESS_CONFLICT`（"占用中的铺位不可停用"）。注意它依据的是**铺位 `OCCUPIED` 字段**而非学员状态集合——与入营/listRooms 的判定口径不同，漂移数据下会出现"有学员挂着的 AVAILABLE 铺可以被停用"或"无学员的 OCCUPIED 铺不让停用"的边角。
   - 反向操作（`DISABLED → AVAILABLE`）任何一层都不拦。

## 四、前端"离营外出"门控 vs 后端 `outing` 校验

1. **条件本身等价，但实现是两套**。`validators.canOuting` 为 `customer.status === 'RESIDENT'`，[CustomersView.vue](file:///d:/Agsb731/Agsb825/gsb3971/frontend/src/pages/CustomersView.vue) 按钮的 `:disabled="row.status !== 'RESIDENT'"` 是内联表达式，**并未引用 `canOuting`**（该函数目前只被 `validators.test.ts` 使用）。后端 `outing()` 同样要求 `RESIDENT`。三处条件字面一致，但前端两处实现各自独立，改一处不会联动另一处，存在日后口径分叉的隐患。

2. **仅后端能拦住的例子**：前端禁用只是 UI 态，绕过的方式很多——直接调 `PATCH /customers/{id}/lifecycle {action:"outing"}` 对一个 `DRAFT`/`DISCHARGED` 学员，前端按钮根本不参与，只有后端 409 `CUSTOMER_STATUS_INVALID` 能拦；或两个浏览器标签页，A 页先对该学员办了结营，B 页列表未刷新按钮仍可用，点击后同样靠后端兜底。

3. **仅前端"拦住"的例子**：正常流程下不存在后端放行而前端禁止的外出场景（条件相同）；前端的禁用只是体验层防线，且 `quickOuting` 成功后的成功提示还误写为"离营离营外出登记成功"。真正的不对称在于：数据漂移时 `outing` 对非 `RESIDENT` 一律 409，而前端按钮在列表数据陈旧时可能处于可点状态——拦不住，只能靠后端。另外前端外出成功后只 `fetchCustomers()` 不刷铺位列表，这恰好与"外出不释放铺位"的服务端语义自洽（铺位无变化无需重取），而入营/结营后是 `Promise.all([fetchCustomers(), fetchBeds()])` 双刷新，保证学员表与铺位表视图同步。

## 五、汇总表

| 类名 | 方法名 | 关键校验点 |
| --- | --- | --- |
| CustomerService | applyLifecycle | 登录 → 学员存在(404) → action 白名单(400)；@Transactional 保证学员/铺位写入同生共死 |
| CustomerService | checkin | bedId 必填(400) → 铺位存在(404) → 非 DISABLED(409) → 无他人 RESIDENT/OUTING 占用(409 BED_OCCUPIED)；不校验学员当前状态，换铺释放旧铺但跳过 DISABLED |
| CustomerService | discharge | 学员状态须 ∈ {RESIDENT, OUTING}(409 CUSTOMER_STATUS_INVALID)；释放铺位时 DISABLED 保持不动 |
| CustomerService | outing | 仅 RESIDENT 可外出(409)；只改学员状态，不释放铺位 |
| RoomService | listRooms | 占用学员按"bedId 非空 且 status ∈ {RESIDENT, OUTING}"聚合挂到 BedView，与入营判定同集合同口径 |
| RoomService | saveBed | 更新分支拦"OCCUPIED 铺 → DISABLED"(409 BUSINESS_CONFLICT)，依据 bed.status 字段而非学员状态集合 |
| CustomersView.vue | quickOuting / 按钮 disabled | 内联 `row.status !== 'RESIDENT'` 禁用，与后端条件等价但属 UI 态可被绕过；外出后仅刷新学员列表 |
| validators.ts | canOuting | `status === 'RESIDENT'`，目前仅测试引用，未接入 CustomersView，与视图内联条件形成双份实现 |
